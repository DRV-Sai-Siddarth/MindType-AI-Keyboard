<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MindType AI"
        android:supportsRtl="true"
        android:theme="@style/Theme.MindTypeAIKeyboard">

        <!-- Main Companion Settings App -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- System Keyboard Service (Main Process) -->
        <service
            android:name=".keyboard.CleverKeyboardService"
            android:exported="true"
            android:label="MindType AI Keyboard"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- Background AI Service (Runs on isolated :ai_engine process) -->
        <service
            android:name=".ai.AIEngineService"
            android:enabled="true"
            android:exported="false"
            android:process=":ai_engine" />

        <!-- File Provider for Custom Stickers/GIFs -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>